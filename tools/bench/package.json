{
  "name": "mtg-forge-ts-bench",
  "version": "0.0.0",
  "private": true,
  "description": "Performance benchmark suite for the @mtg-forge-ts engine. Runs N iterations of cast/resolve/teardown on representative scenarios and reports throughput + latency percentiles. Establishes a baseline for regression detection.",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "mtg-forge-ts-bench": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mtg-forge-ts/cards": "workspace:*",
    "@mtg-forge-ts/core": "workspace:*",
    "@mtg-forge-ts/game": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  }
}
